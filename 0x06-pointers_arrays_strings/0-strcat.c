#include "main.h"
#include <string.h>

/**
 * _strcat - Concatenate strings
 * @dest: holds a string
 * @src: holds a string
 * Descriprition - concatenate strings
 * Return: Always 0 success
 */
char *_strcat(char *dest, char *src)
{
*strcat(*dest, *src);
}
